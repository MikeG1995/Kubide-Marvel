import { ToScrollDirective } from './to-scroll.directive';

describe('ToScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ToScrollDirective();
    expect(directive).toBeTruthy();
  });
});
